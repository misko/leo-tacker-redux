# Architecture decision records

Public contract changes require an ADR before implementation. Each ADR records
the affected producers and consumers, compatibility impact, migration, and the
golden fixtures that change.
